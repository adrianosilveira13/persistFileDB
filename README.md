## Usage

/api/save - Save data into a file

```
{
  "data": {...}
}
```

/api/insert - Persist data into a database

## ENV SETUP
```
DATABASE,
DATABASE_HOST,
DATABASE_PORT,
DATABASE_USERNAME,
DATABASE_PASSWORD,
PORT=(optional)
```